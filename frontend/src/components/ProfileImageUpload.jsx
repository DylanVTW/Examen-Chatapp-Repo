function ProfileImageUpload({ onUploadSuccess }) {
	return (
		<div>
			<p>Upload functionaliteit komt hier.</p>
			<button type="button" onClick={onUploadSuccess}>
				Vernieuw profiel
			</button>
		</div>
	);
}

export default ProfileImageUpload;
